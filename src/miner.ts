import Axios from "axios";
// in this file, i will be making requests to the blockchain and to the backend server together
``;
// connect to database and get all users that have ...@randnotex.com in their email and store them in array, also store their keys.

// const func = () =>{
//     Axios.get( `http://localhost:8024/transactionWebsite/${user.id}`)
//         .then((res) => {
//             if (res.status == 200) {

//             }
//         })
//         .catch((err) => {
//             console.log(err);
//         });
// }
